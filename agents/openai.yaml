interface:
  display_name: "Git 提交助手"
  short_description: "跨平台安全分析 staged 改动并生成或执行规范化 Git 提交"
  default_prompt: "Use $git-commit-helper to inspect staged changes and safely generate or execute a Conventional Commit in the repository's native Git environment."
