# Commit Message Reference

只在需要判断改动意图、影响、`type`、`scope`、正文重点或询问门槛时读取本文件。

## 1. 规则优先级

按以下顺序决定格式：

1. 仓库的 `CONTRIBUTING*`、commitlint 或提交模板。
2. 最近 20 条提交形成的稳定惯例。
3. 本文件的默认 Conventional Commit 规则。

不要为了套用中文 scope 覆盖仓库已经稳定使用的英文或无 scope 风格。

## 2. 意图与影响判定

### 2.1 必须回答的语义

生成 message 前先在内部回答：

| 语义 | 要回答的问题 | 不合格答案 |
|---|---|---|
| 目标 | 为什么需要这批改动？ | “修改了配置”“更新了代码” |
| 行为变化 | 使用者或系统从什么行为变成什么行为？ | 只列文件、函数或字段 |
| 影响 | 谁会观察到变化，兼容性或运行特征是否改变？ | 无证据的“提升体验”“增强稳定性” |
| 证据 | 哪些上下文、测试、调用关系或契约支撑结论？ | 只凭文件名猜测 |

“目的”可以是业务目标、缺陷消除、约束落实或内部结构目标。纯重构的目的可以是收敛职责或解除耦合，但必须有 patch 与调用关系支撑，不能把任何代码移动都包装成架构收益。

### 2.2 证据优先级

1. 用户当前请求、已批准方案、issue/任务描述。
2. 变化的测试、断言、注释、文档、配置或迁移语义。
3. 变更符号周边代码、直接调用方、API/UI/数据契约。
4. 与变更路径或符号直接相关的近期历史。
5. staged diff 中的实现动作。

高优先级证据解释“为什么”，低优先级证据验证“是否真的这样改了”。历史上下文只能解释 staged 内容，不能把未暂存或尚未实现的计划写入提交。

staged 非空时，使用 `git show :<path>` 与 `git grep --cached` 读取 index 版本，使用 `git show HEAD:<path>` 查看改动前版本。未 staged 路径若不干净，只能读取 `HEAD` 版本；unstaged/untracked 内容不能作为 staged message 的事实证据。

### 2.3 零交互判定

| 当前证据状态 | 动作 |
|---|---|
| 唯一合理意图，影响明确 | 直接生成或执行，不询问 |
| 动机未知，行为结果唯一 | 使用保守的结果表述，不编造动机，不询问 |
| 多个解释共享同一提交语义 | 写共同且可证明的结果，不询问 |
| 多个合理解释会改变 `type`、`subject`、范围或影响/风险 | 只问一个能区分解释的具体问题 |
| staged 包含多个无法归并的目的 | 停止执行并请用户准备一个连贯 staged 子集 |

询问时给出已经发现的具体分歧，例如：

> 这次 `price` 改为 `amount` 是有意发布破坏性 API 变更，还是需要继续兼容旧字段 `price`？

不要问“这些改动是做什么的？”或要求用户重新解释整个任务。

## 3. `type` 判定

| type | 核心意图 |
|---|---|
| `feat` | 新功能、新能力或新模块 |
| `fix` | 修复错误行为、安全问题或边界缺陷 |
| `refactor` | 调整内部结构且外部行为不变 |
| `perf` | 性能优化 |
| `test` | 仅新增或调整测试 |
| `docs` | 仅更新文档 |
| `build` | 构建系统或依赖管理 |
| `ci` | CI/CD 流程 |
| `chore` | 其他维护性工作 |
| `revert` | 回滚既有提交 |

按主要业务意图选择，不按文件数量或扩展名选择。

只有配置值、常量或默认值发生变化，且没有证据表明它在引入能力或修复缺陷时，遵循仓库惯例；仓库没有稳定惯例时使用 `chore`，不要仅因行为数值变化就猜成 `feat` 或 `fix`。

## 4. `scope` 与 `subject`

- scope 可省略；有明确业务域时优先使用具体业务名，例如 `商品选项`、`订单状态`、`认证`。
- 不用 `工具函数`、`数据库`、`gorm` 等实现手段冒充业务域，除非仓库惯例如此。
- subject 概括目的或可观察结果，避免“更新代码”“调整逻辑”“修改字段”“fix bug”。

示例：

```text
fix(订单状态): 兼容格式不一致的状态值
refactor(认证): 收敛登录态恢复入口
docs: 补充生产部署步骤
```

## 5. 正文

正文按以下顺序排列，并且每一项都能由 staged patch 与相关证据证明：

1. 核心行为变化及主要影响。
2. 必要的接口、兼容、数据或验证证据。
3. 锁文件、生成文件等被动更新。

标题与正文之间保留一个空行；bullet 之间不留空行。

正文第一条使用“受影响对象或条件 + 可观察行为”的结构。例如，“未显式指定分页数量的列表每页默认返回 50 条”是影响；“把 `DefaultPageSize` 从 20 改为 50”只是实现动作。

实现细节不是默认主语。只有它能解释影响、兼容性或验证方式时，才写入正文。

## 6. 范围与询问门槛

只有出现 **ambiguous scope** 或多个会改变提交语义的合理意图时请求额外信息，例如：

- `execute-mode` 下 staged 为空，且用户没有指定路径。
- `command-only` 下 staged 为空，且用户没有指定要暂存的路径。
- staged 同时包含明显无关的多个主题。
- 用户说“这些改动”，但当前目录存在多个可能仓库或多个独立 staged 批次。
- 需要把 untracked 文件加入提交，但文件可能包含配置、凭据或本地数据。
- 从 Windows 操作 WSL 仓库，但存在多个发行版且目标发行版无法从上下文确定。
- 同一 API 字段变更既可能是有意 breaking change，也可能遗漏兼容层。
- 同一 staged 批次同时支持两个无法用一个目的解释的行为变化。

混合 staged 范围下不要自动 `reset`、`restore --staged` 或重建 index；这些操作可能丢失用户的部分暂存边界。停止执行，说明应拆分的主题，等待用户准备一个连贯 staged 子集后重新运行 `status`、staged stat/name-status/check、完整 diff 与意图分析。

以下情况不重复确认：

- 用户明确说“提交git”“帮我提交”或“按这个提交”。
- staged 范围单一且与当前任务一致。
- 用户已经确认了 message 或明确指定了提交路径。
- 用户没有亲口说明目的，但测试、调用关系或契约支持唯一合理解释。
- 商业动机未知，但唯一可观察结果足以写出准确、保守的 message。

## 7. 最终检查

- staged 范围与用户意图一致。
- 已形成目标、行为变化、影响和证据，不是 diff 的自然语言复述。
- 只有多个语义不同的合理解释仍并存时才询问，而且只问了最小必要信息。
- type 按核心意图选择。
- subject 表达目的或可观察结果，没有编造收益。
- 正文第一条同时包含受影响对象或条件与可观察行为，不是数值或字段变更复述。
- 正文先写行为及影响，再写必要的实现或验证证据。
- message 没有描述 unstaged 内容。
- `git diff --cached --check` 已通过。
- 相关测试、构建或静态检查已完成。
- 检查、测试、临时 index、hooks 与 commit 使用同一个 Git 运行环境，没有混用 Windows Git 和 WSL Git。
- 当前平台执行器隔离并校验 staged tree；仅在 commit、父关系与 symbolic HEAD 均未漂移时对原分支 ref 做 CAS 撤回，并发 HEAD 或分支切换时不自动改写引用。
- 执行成功后已回读 `%H%n%B` 和最终 status。
